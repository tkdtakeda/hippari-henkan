以下は依頼者から明示された設計ルール。UI を作る・直すときは毎回これに照らして確認すること。

### 1. 面積は「頻度 × 重要度」で配る
よく見るもの・重要なものほど画面を大きく占める。たまにしか使わない設定や解説は、
折りたたみ・ダイアログ・メニューに追いやる。全部を同じ大きさで並べない。

### 2. 次にすることを常に 1 つだけ指す（探させない・数えさせない）
主要動作（CTA）は画面に 1 つ。位置は固定して、状態によってラベルだけ変える。
件数や合計は必ずこちらで数えて文言に入れる（「4 件を変換する」）。
ユーザーに探させない・数えさせない。

### 3. 状態は色だけで伝えない（必ず文字を添える）
成功・警告・失敗は「アイコン ＋ 文字 ＋ 色」の三重表現。
色覚特性やモノクロ表示でも意味が伝わること。

### 4. できないことは、できないと書く（押せるのに何も起きないボタンを残さない）
無効な操作は disabled にしたうえで、その理由を必ず画面に出す。
押せるのに反応しないボタン、押せるが失敗するボタンを残さない。

### 5. 危ない操作を主要動線に置かない
全削除・不可逆な操作は主要動線から外す（オーバーフローメニュー等）。
実行時は確認ダイアログではなく「取り消し」を用意する。

### 6. 出どころ・単位・根拠を画面に出す
数値には必ず単位を付ける。判定結果には根拠（ハッシュ・文字コード・検証方法）を併記する。
「なぜそう表示されているか」を画面上で追えるようにする。

### 7. 見た目の値はトークンから選ぶ
色・余白・角丸・文字サイズ・高さ・アニメーション時間は CSS カスタムプロパティで定義し、
コンポーネント側は必ずトークンを参照する。生の px やカラーコードを直接書かない。

### レイアウト方針
スクロールレス。ページ自体はスクロールさせず `100dvh` に収める。
件数が増える一覧などは、その領域の内側だけをスクロールさせる。