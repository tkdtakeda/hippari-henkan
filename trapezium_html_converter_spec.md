# 引張試験ファイル 変換＋解析ツール（ブラウザ単体版）実装仕様書

外部AI向け実装指示書 / rev.2
対象成果物: **単一のHTMLファイル**（`file://` でローカル起動し、サーバ不要）

> rev.2 追記: 「元データの読み込み → CSV変換」に加え、**ひずみ-応力線図 / 時間-応力線図の作図**と、**引張強さ・耐力・伸び・応力増加速度**の算出（＝解析機能）を §11〜§14 に追加した。

---

## 0. この仕様書について

- 本仕様書は、島津 TrapeziumX / TrapeziumX-V が出力する試験ファイル（`.xtux` / `.vtav`）を、ブラウザ上でCSVへ変換するツールを実装するための指示書である。
- 既存のPython版パーサの解析ロジックを、ブラウザ（JavaScript）へ移植する。数値・処理結果はPython版と一致させること。
- **本仕様書には特定企業・個人・実測データ・実ファイル名・社内URLは含まれない。** ここに記載のバイト構造・正規表現は、フォーマット解析のために必要な技術情報である。
- 外部ライブラリはCDN依存を避け、**単一HTML内にバンドル**すること（オフライン `file://` 実行を保証）。Zip生成が必要なため、Zip生成のみ最小実装を内包する（詳細は §7）。

---

## 1. 全体要件

| 項目 | 要件 |
|---|---|
| 実行形態 | 単一HTMLファイル。`file://` でダブルクリック起動、ネットワーク通信一切なし |
| 対応ブラウザ | 最新の Chromium系 / Firefox（`TextDecoder`, `DataView`, `Blob`, `URL.createObjectURL` 使用可） |
| 入力方式 | (a) ドラッグ&ドロップ領域、(b) `<input type="file" multiple>` の**両対応**。複数ファイル同時可 |
| 入力ファイル | `.xtux`, `.vtav`。拡張子が違ってもマジック判定を優先（§3） |
| 出力 | 各入力ファイルにつき CSV を最大4種生成（report / results / conditions / waveform）＋任意で audit |
| ダウンロード | **(1) ファイル単位の分割DL、(2) 全結果のZip一括DL の両方を必須実装**（§6） |
| 画面表示 | 変換後、ファイルごとに結果テーブルを画面表示（§8） |
| 文字コード | 出力CSVは **UTF-8 BOM付き**（Excelで文字化けしないこと） |
| 依存 | 外部ネットワークライブラリ禁止。Zipは内蔵実装（§7） |

---

## 2. 入力→出力の全体フロー

各入力ファイルについて以下を実行する。

1. `File` を `ArrayBuffer` として読み込む（`FileReader.readAsArrayBuffer`）。以後 `data`（`Uint8Array`）として扱う。
2. フォーマット判別（§3）→ `xtux` or `vtav`。
3. TLV文字列トークン抽出（§4）。トークンは `{offset:int, text:string}` の配列。
4. 結果サマリー抽出（§5.1: xtux / §5.2: vtav）。
5. レポート項目マッピング（§5.3）。
6. 試験条件抽出（§5.4）。xtux時は寸法を別ルートで補完（§5.4.2）。
7. 生波形抽出（§5.5）。
8. （任意）変更履歴抽出（§5.6, vtavのみ）。
9. 出力CSVをメモリ上に生成し、画面表示＋DL対象として保持。

---

## 3. フォーマット判別

```
先頭64バイト中に ASCII "TXVFileVersion" が存在すれば "vtav"
それ以外は、拡張子が ".vtav" なら "vtav"、そうでなければ "xtux"
```

- 判定順は「マジック優先 → 拡張子」。

---

## 4. TLV文字列トークン抽出（共通処理の心臓部）

ファイル内の文字列は **TLV（Type/Length/Value）** で格納される。フォーマットにより方式が異なる。

### 4.1 xtux 文字列（UTF-16LE）

- 走査: バイト列を先頭から1バイトずつ見る。
- マーカー: `0x3E`（`>`）。
- 直後の1バイト `L` が **長さ（バイト数）**。条件 `2 ≤ L ≤ 254 かつ L が偶数`。
- 続く `L` バイトを **UTF-16LE** としてデコード。
- 採否フィルタ:
  - 各文字は `コードポイント ≥ 0x20`、または `\n \r \t` のいずれか。
  - `0xFFFE, 0xFFFF` を含む文字列は破棄。
  - デコード後、`\n \r \t` を除去し前後空白を trim（★ラベルは表示折返し用に改行/タブが埋め込まれている場合があるため、丸ごと破棄せず除去する）。
  - 除去後の文字列が下記「許可文字クラス」に**完全一致**する場合のみ採用。
- マッチしたら読み取り位置を `+ (2 + L)` 進める。非マッチなら `+1`。

**許可文字クラス（正規表現・完全一致）**:
```
[\u3000-\u30FF\u4E00-\u9FFF\uFF00-\uFFEF々ー_A-Za-z0-9/%.\^+\-()\[\] :×]+
```

### 4.2 vtav 文字列（UTF-8）

- 走査: 先頭から1バイトずつ。
- 直後方式ではなく、**現在バイト `L` を長さとみなす**（`1 ≤ L ≤ 250 かつ 現在位置+1+L ≤ 全長`）。
- 続く `L` バイトを **UTF-8** としてデコード（失敗したら `+1` して継続）。
- 採否フィルタ:
  - `length ≥ 1`、各文字は `≥ 0x20` または `\t`。
  - `[\u3000-\u9FFF\uFF00-\uFFEFA-Za-z0-9]` のいずれかを最低1文字含む。
  - 前後空白 trim。
- 採用時は `+ (1 + L)`、非採用時は `+1`。

> 実装メモ: UTF-8/UTF-16LEのデコードは `TextDecoder('utf-8', {fatal:true})` / `TextDecoder('utf-16le', {fatal:true})` を使い、例外を捕捉して非採用判定にすること。

---

## 5. 抽出ロジック詳細

### 5.0 定数

```
UNITS = { "N", "N/mm2", "mm", "sec", "%", "MPa/s", "/sec", "mm/min", "GPa", "kN", "MPa" }
NUM   = /^-?\d+(\.\d+)?$/       // 数値判定
CJK   = /[\u3040-\u30FF\u4E00-\u9FFF]/

COND_KEYS = ["品名","試験片名","厚さ","幅","直径","平行部長さ","つかみ具間距離",
             "ロードセル容量","試験機種類","作成者","検討者","承認者",
             "作成日","試験日","試験モード","試験種類","速度","試験片形状",
             "バッチ数","サブバッチ数"]
```

### 5.1 結果サマリー抽出（xtux：順序構造）

トークンの text 配列 `toks` を先頭から走査。

1. `toks[i] === "名前"` を見つけたら、そこから `"単位"` が現れる位置 `j` まで進める。
2. `k = j + 1` からラベル・単位・パラメータの行を組み立てる:
   - `t = toks[k]` が `NUM` に一致、または `"名前"` なら行組み立て終了。
   - `cur` にトークンを push。`t` が `UNITS` に含まれたら、その時点で
     `row = { label: cur[0], unit: t, param: cur[1..-2] を空白連結 }` を確定し、`cur` を空に。
3. 続いて `toks[k]` が `NUM` の間、値 `vals[]` として収集。
4. `rows[idx]` と `vals[idx]` を対応づけ、`{label, unit, value, param}` を結果に追加。
5. 補完: `label === "" かつ unit === "mm/min"` の行は `label = "実績クロスヘッド変異速度"` とする。

### 5.2 結果サマリー抽出（vtav：バイトアンカー方式）★確度高

vtavは値がバイト位置で確定できる。

**値アンカー（数値）正規表現（バイト列に対して）**:
```
([\x01-\x0f])(-?[0-9][0-9.\-]{0,14})\x00[\x01\x02]\x00\x00\x00
```
- group1 = 長さ1バイト、group2 = 値ASCII。
- **採用条件: group1 の数値 === group2 のバイト長**（長さ整合チェック）。

**未算出（N/A）アンカー正規表現**:
```
\x03(-\.-)\x00[\x01\x02]\x00\x00\x00
```
- ヒット時の値は文字列 `"N/A"` とする。

**範囲限定**:
- `results_start` = バイト列中で最初に一致する `\d{4}/\d{2}/\d{2} \d{1,2}:\d{2}:\d{2}`（日時）の位置。無ければ0。
- `audit_start` = バイト列中の `"試験条件"`(UTF-8) の最初の出現位置。無ければ全長。
- 値アンカーは `results_start ≤ offset < audit_start` の範囲のみ採用。

**ラベル対応づけ**:
- ラベル候補 = トークンのうち `isResultLabel(text)===true`（§5.2.1）。
- 各値アンカー `offset` に対し、`offset より前で 距離 < 200バイト` のラベルのうち**最も後ろ（offsetに近い）**を選ぶ。
- 一度使ったラベル offset は再利用しない（`seen` で重複排除）。
- 値が `"0"` のアンカーはスキップ。
- 単位 = `nearestUnit`（§5.2.2）。

#### 5.2.1 `isResultLabel(s)`
```
CJKを含まない → false
s === "全エリアで計算" → false
s が "計算式" で始まる / "速度センサー" で始まる → false
s が "Arial" を含む / "[" で始まる / "<<" で始まる / "->" を含む
   / "(" で始まる / "]" を含む / "@" を含む → false
以下のいずれかを満たせば true:
   "_" を含む / "×" を含む / "点" を含む / "率" で終わる / "速度" を含む / "ヤング" を含む
それ以外 false
```

#### 5.2.2 `nearestUnit(units, voff)`
- `units` = トークン中 `text ∈ UNITS` を offset昇順に並べたもの。
- `voff` より前で、かつ `voff - o < 200` を満たす最後の単位トークンの text を返す。無ければ空文字。

> バイト列に対する正規表現マッチは、JSでは「バイトを1文字=1コードポイント(0–255)に写した latin1 文字列」を作り、その上で `String.prototype.matchAll` を使うと安全。マッチ位置＝バイトオフセットとして扱える。

### 5.3 レポート項目マッピング（build_report）

結果リストを `label → 最初の出現` の辞書 `by_label` にする（**先勝ち**）。以下の標準項目を、候補ラベルを優先順で探して値を採用する。

| 標準項目名 | 単位 | 候補ラベル（優先順） |
|---|---|---|
| 引張強さ Rm | N/mm2 | 最大点_応力, 最大点_Rm |
| 耐力 Rp0.2 | N/mm2 | 耐力点1_応力, 耐力点1Rp |
| 伸び | % | 破断点_変位(ひずみ), 破断点_At |
| 応力増加速度 | MPa/s | 応力増加速度 |
| ひずみ速度 | /sec | 歪速度 |
| クロスヘッド変位速度 | mm/min | 実績クロスヘッド変異速度, クロスヘッド変異速度 |
| 弾性率 | N/mm2 | 弾性率_Standard |
| ヤング率 | GPa | ヤング率 |
| 最大試験力 Fm | N | 最大点_試験力, 最大点_Fm |
| 耐力点試験力 | N | 耐力点1_試験力 |

- 見つからない項目は値を空、`source_label` を空にする。
- 出力行: `item, value, unit, source_label`。

### 5.4 試験条件抽出（extract_conditions）

トークン text 配列 `vals` を走査。

1. `s ∈ COND_KEYS` かつ未取得なら、`s` の直後〜最大5トークン先までを候補として順に見る:
   - 候補が空 or `s` と同一ならスキップ。
   - 候補が `COND_KEYS` に含まれたらそのキーの探索を打ち切り（break）。
   - `validateCond(s, cand)`（§5.4.1）が非nullなら採用して break。
2. ロット/製造番号: トークン中で `^[A-Z][0-9A-Z]{6,}$` に**完全一致**する最初のものを `cond["ロット/製造番号"]` とする。

#### 5.4.1 `validateCond(key, cand)`
```
key ∈ {厚さ,幅,直径,平行部長さ,つかみ具間距離} → /^-?\d+\.\d+$/ に一致すれば cand、else null
key ∈ {作成日,試験日}                         → /^\d{4}\/\d{2}\/\d{2}( \d{1,2}:\d{2}:\d{2})?$/ に一致すれば cand、else null
key === ロードセル容量                        → /^\d+\s?kN$/ に一致すれば cand、else null
key ∈ {バッチ数,サブバッチ数}                 → /^\d+$/ に一致すれば cand、else null
上記以外                                       → cand が _NONVALUE に含まれなければ cand、else null
```

`_NONVALUE` = `COND_KEYS` ∪ `UNITS` ∪
```
{ "タイトル","キーワード","オペレータ","コメント","結果ファイル名","条件ファイル名",
  "温度","湿度","機体番号","伸び原点","名前","パラメータ","合否判定","単位",
  "変位計1標点距離","変位計1リミット","変位計1フルスケール","変位計2フルスケール",
  "幅計標点距離","幅計フルスケール" }
```

#### 5.4.2 xtux 寸法補完（重要）

xtuxでは寸法（厚さ/幅/直径/平行部長さ/つかみ具間距離）が UTF-16LE 本体ではなく、**UTF-8 TLVの寸法セクション**に埋め込まれる。したがって xtux の場合、§4.2（UTF-8ウォーカー）を**同じdataに対して別途実行**し、以下で補完する。

- 並びは `[ラベル][mm][フラグ"1"][小数値]`（例: `厚さ mm 1 11.843`）。
- 各寸法ラベルの直後〜最大5トークン先を見て:
  - 候補が寸法ラベル or `{試験片名,名前,パラメータ,合否判定,単位}` なら break。
  - 候補が `^-?\d+\.\d+$`（**小数のみ**。フラグ"1"を除外）なら採用して break。

### 5.5 生波形抽出（extract_waveform）

- マーカー `MARKER = 0x01 00 00 00 00 01 01`（7バイト）の全出現位置 `pos[]` を取得。
- `pos.length < 20` なら波形なし（null）。
- 連続差分 `diffs[i] = pos[i+1]-pos[i]`。`0 < d < 4096` の中で**最頻値**を `stride` とする。
- `good` = `pos[i+1]-pos[i] === stride` を満たす `pos[i]` の集合。末尾 `pos[-1]` も `pos[-1]-good[-1]===stride` なら追加。
- `good.length < 20` なら null。
- チャネル定義（マーカー位置 `p` からのバイトオフセット、float32 LE）:

| 列名 | オフセット |
|---|---|
| Time_sec | -8 |
| Stroke_mm | -4 |
| Force_N | +29 |
| Extensometer_mm | +37 |

- 各 `good` 位置について `p+off` から 4バイトを `float32(LE)` として読み、列を作る。
- 列は「長さが `good.length` と一致 かつ 全要素が NaN でない」場合のみ採用。
- 出力は行番号 `#` + 採用列。数値は小数6桁に丸める。

### 5.6 変更履歴抽出（parse_audit, vtavのみ・任意）

- トークン text を走査。`text` が `"試験条件"` を含み `"<<"` で始まる行を項目とみなす。
- 直後トークンを暫定 change とし、`i+1..i+3` の範囲で `^\d{4}/\d{2}/\d{2} \d{1,2}:\d{2}:\d{2}$` に一致する日時を探す。
  - 見つかった位置が `i+1` なら change を空にする。日時の次トークンを user とする。
- 出力行: `item, change, datetime, user`。

---

## 6. 出力CSV仕様

すべて **UTF-8 BOM付き**、区切りカンマ、改行 `\r\n`。ファイル名の `<name>` は入力ファイルの拡張子を除いた basename。

| ファイル | ヘッダ | 生成条件 |
|---|---|---|
| `<name>_report.csv` | `item,value,unit,source_label` | 常に生成 |
| `<name>_results.csv` | `label,value,unit,param,confidence` | 常に生成。`confidence` は vtav=`high(byte-anchor)` / xtux=`high(order)` |
| `<name>_conditions.csv` | `key,value` | 常に生成 |
| `<name>_waveform.csv` | `#,<採用列名...>` | 波形が採用された場合のみ |
| `<name>_audit.csv` | `item,change,datetime,user` | vtav かつ「変更履歴を出力」ONで行がある場合のみ |

- CSVフィールドはカンマ・改行・ダブルクオートを含む場合に `"` で囲み、内部の `"` は `""` にエスケープすること。

---

## 7. ダウンロード要件（分割・一括の両立）★必須

画面には最低2つのDL手段を用意する。

### 7.1 分割DL
- 各入力ファイルの結果ブロックごとに、生成された各CSVの個別ダウンロードボタンを表示。
- `Blob([BOM, csvText], {type:"text/csv"})` → `URL.createObjectURL` → `<a download>` クリックで保存。使用後 `URL.revokeObjectURL`。

### 7.2 Zip一括DL
- 「すべてZipでダウンロード」ボタンを1つ用意。
- Zip内のフォルダ構成:
  ```
  <name1>/<name1>_report.csv, _results.csv, _conditions.csv, _waveform.csv, (_audit.csv)
  <name2>/...
  ```
- **Zipは外部ライブラリを使わず内蔵実装**（オフライン保証のため）。要件:
  - 圧縮方式は **store（無圧縮, method=0）** で可（CSVは小さく、実装簡素・可搬性優先）。
  - 各ファイルにローカルファイルヘッダ + 中央ディレクトリ + End Of Central Directory を正しく出力。
  - CRC-32 を各エントリで計算（標準多項式 `0xEDB88320`）。
  - ファイル名は UTF-8。可能なら汎用フラグbit11（UTF-8）を立てる。
  - 生成した `Uint8Array` を `Blob({type:"application/zip"})` にしてDL。
- 実装が難しい場合でも **CDN取得は禁止**。JSZip等を使うなら**ソースをHTMLに直接インライン**して同梱すること。

---

## 8. 画面表示（UI）

- **ドロップゾーン**（大きめの点線枠、`dragover`でハイライト）＋ **「ファイルを選択」ボタン**（`<input type="file" multiple accept=".xtux,.vtav">`）。
- 変換後、入力ファイルごとにカード表示:
  - ヘッダ: ファイル名 / 判別フォーマット / サイズ(bytes) / 検出結果数 / 波形点数。
  - **レポート項目テーブル**（item / value / unit）。値が空は `(なし)`、`"N/A"` はそのまま表示。
  - 主要条件（ロット/製造番号, 厚さ, 幅, 平行部長さ, 試験片形状, 試験日）があれば表示。
  - 各CSVの分割DLボタン。
- 画面上部または下部に「すべてZipでDL」ボタン。
- オプション: チェックボックス「変更履歴(audit)も出力（vtavのみ）」。
- エラー時（判別不能・デコード失敗）はカードにエラーメッセージを表示し、他ファイルの処理は継続。

---

## 9. 検証（受け入れ条件）

- 同一入力に対し、Python版パーサの `report.csv` / `results.csv` / `conditions.csv` / `waveform.csv` と**値が一致**すること（数値は表示桁一致、波形は小数6桁一致）。
- vtavで `Force_N` の最大値が波形の最大値と整合すること。
- `.xtux` / `.vtav` 両方で動作。拡張子偽装（中身vtav・拡張子xtux 等）でもマジック判定で正しく処理。
- 複数ファイル投入時、分割DL・Zip一括DLの双方が正しく機能。
- ネットワークを切断した `file://` 環境で全機能が動作すること。

---

## 10. 実装上の注意（JS移植のヒント）

- バイナリは `Uint8Array`。float32読み出しは `DataView.getFloat32(offset, /*littleEndian=*/true)`。
- バイト列への正規表現は、バイトを `String.fromCharCode` で 0–255 の1文字ずつに写した「latin1文字列」を作って適用し、`match.index` をバイトオフセットとして使う。
- UTF-16LE / UTF-8 のデコードは `TextDecoder`。不正バイトは `{fatal:true}` で例外化し、非採用に倒す。
- 大きな波形ファイルに備え、トークン走査・正規表現は1パスで済ませ、O(n)を意識する。
- 出力の文字化け防止のため、CSV先頭に UTF-8 BOM (`\uFEFF` 相当のバイト `EF BB BF`) を必ず付与。

---

# ── 解析機能（rev.2 追加） ──

## 11. 解析の全体像

変換で得た（または直接読み込んだ）測定データから、以下を行う。

1. 生データ列（時間・試験力・変位・ストローク）から **応力 σ** と **ひずみ ε** を計算。
2. **ひずみ-応力線図（SS曲線）** と **時間-応力線図** を作図。
3. 主要な機械的特性を算出:
   - **引張強さ Rm**
   - **耐力**（速度法による降伏点／0.2%オフセット耐力の2系統）
   - **伸び**（破断ひずみ）
   - **応力増加速度**
   - 参考: ヤング率、ひずみ速度①②
4. 各測定点マーカー（耐力点・破断点・0.2%耐力）を線図上に重畳表示。
5. 結果を一覧表＋CSVで出力。

### 11.1 入力データモデル

解析エンジンは1行=1サンプリング点のレコード列を受け取る。各レコードの役割（role）は以下。

| role | 意味 | 単位 | 必須 |
|---|---|---|---|
| `time` | 経過時間 | sec | ○（速度・応力増加速度・ひずみ速度に必要） |
| `force` | 試験力 | N | ○ |
| `stress` | 応力 σ | N/mm² | 入力経路により取得方法が異なる（§11.2）。CSV入力は行ごとに保持、DAT入力は §11.2 で復元 |
| `displacement` | 変位計（伸び計）値 | mm | ○（ひずみ算出に必要） |
| `stroke` | クロスヘッドストローク | mm | △（速度算出に使用。無ければ速度法耐力は不可） |

- CSVから読み込む場合は列マッピング（プリセット）で各roleに列を割り当てる。エンコーディングは UTF-8 / Shift-JIS / EUC-JP を自動判別（BOM→UTF-8 strict→Shift-JIS→EUC-JP の順で試行）。

### 11.2 応力の取得（★入力経路で2通り。応力情報は保持されている）

応力は「無い」のではなく、**入力の種類によって取得経路が異なる**。実データ検証済みの事実として、以下を厳守する。

**(A) CSV入力（Trapezium のCSVエクスポート等）**
- 1行=1サンプリング点に **`応力` 列がそのまま含まれる**（既定プリセットの列: 時間 / 試験力 / 応力 / 変位計1 / ストローク）。
- この場合、`stress[i]` は列値をそのまま使用する（換算不要）。

**(B) DAT入力（.xtux / .vtav → 変換部 §4〜§6 経由）**
- DATは応力を **サマリー点として保持**している（例: `最大点_応力`, `耐力点1_応力`, `中間点_応力`）。これらは Trapezium が算出済みの値で、変換部の `results.csv` にそのまま出力される。
- ただし **1点ごとの波形**（`waveform.csv` = `Time_sec, Stroke_mm, Force_N, Extensometer_mm`）には**応力の"列"が無い**。作図用の全点SS曲線を得るには、各点で応力を復元する:
  ```
  stress[i] = force[i] / A          // A = 断面積[mm²]
  ```
- この復元値は **DAT内蔵のサマリー応力と厳密に一致**する（検証: 平板厚1.5×幅24.983でA=37.47mm²、5797N/37.47=154.7≈155＝`最大点_応力`、5524N/37.47=147.4≈147＝`耐力点1_応力`）。したがって force÷A は近似ではなく、Trapezium と同一定義。

**断面積 A の求め方（DAT入力時のみ必要）**
- 平板: `A = 厚さ × 幅`（試験条件 §5.4 から取得）。
- 丸棒: `A = π/4 × 直径²`。
- 条件から取得できない場合はユーザーに A（または厚さ・幅・直径）を入力させる。

> まとめ: **応力は保持されている。** CSV入力なら行ごとの応力列をそのまま使い、DAT入力ならサマリー応力はそのまま取得、全点SS曲線のみ force÷A で復元する（値は内蔵応力と一致）。

---

## 12. 基本量の計算

読み込んだレコード列に対し、最初に以下を全点計算する（`computeAll` 相当）。

### 12.1 ひずみ ε [%]

```
strain[i] = displacement[i] / gaugeLength × 100      // gaugeLength: ゲージ長[mm]（既定 50）
```
- `displacement[i]` が欠損なら strain[i] = null。

### 12.2 速度 v [mm/min]

```
for i = 1..N-1:
  dt = time[i] - time[i-1]
  if dt <= 0 or stroke/time が欠損: skip
  velocity[i] = (stroke[i] - stroke[i-1]) / dt × 60
velocity[0] = velocity[1]
```
- オプションで移動平均（窓幅 `smoothingWindow`、既定5）による平滑化を選択可能。速度は耐力点（速度法）の検出に使う。

---

## 13. 線図の作図

Chart.js 等の折れ線描画ライブラリを用いる（**オフライン化のため本体にバンドル**、CDN参照禁止）。散布/折れ線で `{x, y}` 系列を描く。

### 13.1 軸の選択

| 軸キー | ラベル | 値 |
|---|---|---|
| `strain` | ひずみ ε (%) | strain[i] |
| `displacement` | 変位 (mm) | displacement[i] |
| `time` | 時間 (sec) | time[i] |
| `stress` | 応力 σ (N/mm²) | stress[i] |
| `force` | 試験力 F (N) | force[i] |
| `velocity` | 速度 (mm/min) | velocity[i] |

- **ひずみ-応力線図**: X=`strain`, Y=`stress`（既定）。
- **時間-応力線図**: X=`time`, Y=`stress`。
- 系列は `x, y` いずれかが null/NaN の点を除外して描画。
- 自動スケール: 実データ（SS曲線）の min/max に 4% マージンを付与（回帰直線やオフセット線の外挿点はスケール計算から除外）。

### 13.2 重畳する注釈・マーカー

ひずみ-応力線図（X=strain）のとき、以下を重ねる。

- **耐力点**（速度法）: ▽マーカー（§14.2）。
- **破断点**: ×マーカー（§14.4）。
- **0.2%耐力点**: ★マーカー（§14.3）。
- **回帰直線（弾性勾配）**: 直線域 startStrain〜cutoffStrain を実線、その先を破線で外挿。
- **0.2%オフセット線**: `y = slope × (x − 0.2)` を破線描画（x>0.2 の範囲）。
- **直線域バンド**: startStrain〜cutoffStrain を薄い帯で表示。

---

## 14. 機械的特性の算出

### 14.1 引張強さ Rm（最大応力）

```
maxForce = max(force[]) の値、maxIndex = その位置
Rm = stress[maxIndex]          // = maxForce / A
maxStrain = strain[maxIndex]
```
- Rm は「最大試験力に対応する応力」。線図上の最高点。

### 14.2 耐力①: 速度法（降伏点検出）

クロスヘッド速度が弾性域から急増する点を降伏（耐力）点とみなす。

```
1) maxForce = max(force[])
2) 弾性域の基準速度 vBase:
     force が [0.05·maxForce, 0.30·maxForce] の範囲にある点の velocity の中央値
3) 速度閾値 thr:
     既定 = velocityThreshold（既定5.0）
     vBase が 2.7〜3.3 → thr = 3.3
     vBase が 5.4〜6.6 → thr = 6.6
     （試験速度 3 or 6 mm/min の自動判別）
4) forceFloor = maxForce × (linearStart_pct/100)   // 既定 5%
5) n を昇順に走査し、最初に
       force[n] ≥ forceFloor かつ velocity[n] > thr
   を満たす n を耐力点とする。
   返り値: { index:n, strain, stress, force, velocity, threshold }
```

### 14.3 耐力②: 0.2%オフセット耐力

弾性直線を 0.2% 平行移動した線と SS曲線の交点。

**前提: 弾性勾配 slope の決定（直線域自動検出）**
```
yieldStress = 速度法耐力点の stress
band = [yieldStress·linearityLo, yieldStress·linearityHi]   // 既定 0.2〜0.5
band内の (strain, stress) を最小二乗フィット → slope, intercept
（スムーズネス判定で降伏開始側の非直線点を除外し cutoffIdx を決定）
ヤング率 E = slope × 100  [N/mm²]     // slope は N/mm² per strain% のため ×100
```

**0.2%オフセット交点**
```
offset線: yLine = slope × (strain − 0.2)
strain 昇順に diff = stress − yLine を追跡し、
diff が正→0以下 に変わる点を交点とする（前後の |diff| が小さい側を採用）。
返り値: { index, strain, stress } … この stress が 0.2%耐力。
```
- 交差が見つからない場合は |stress − slope·(strain−0.2)| が最小の点を近似採用。

### 14.4 伸び（破断ひずみ）

破断点を検出し、その点のひずみを伸びとする。検出は2方式（優先順に使用）。

**方式A: 式①→②法**
```
maxForce, maxIndex を求める
式①（急落開始）: n>maxIndex で
     |force[n]-force[n-1]| > maxForce·0.0005  かつ
     |force[n+1]-force[n]| > k1·|force[n]-force[n-1]|    // k1 既定 5
   を満たす最初の n
式②（低荷重確定）: searchStart(=①点 or maxIndex) から
     force[n+1] < k2·maxForce                            // k2 既定 0.02
   を満たす最初の n を破断点とする
返り値: strain（＝伸び）, stress, maxForce, maxStrain, maxStress(=Rm) 等
```

**方式B: 島津法（1秒間の荷重低下率）**
```
threshold = maxForce × (pctOfMaxForce/100)   // 低下率%はプリセット可変
dt = 時刻列から推定したサンプリング周期の中央値（既定0.05s）
stepsPerSecond = round(1/dt)
n ≥ maxIndex で force[n] − force[n+stepsPerSecond] ≥ threshold
   を満たす最初の n を破断点とする
```
- 伸び = 採用した破断点の `strain`。両方式を併記して出力してよい。

### 14.5 応力増加速度 [MPa/s]

弾性直線域における「時間 vs 応力」の最小二乗傾き。

```
直線域 startIdx〜cutoffIdx で (time, stress) を最小二乗フィット
stressRampRate = 傾き [N/mm²/s = MPa/s]
```
- 許容範囲（下限/上限）を設定し、外れたら警告表示（任意）。

### 14.6 参考: ひずみ速度①② [s⁻¹]

```
lsrFit(区間) = 「time vs strain(%)」最小二乗傾き ÷ 100   // %/s → s⁻¹
① 弾性: 直線域 startIdx〜cutoffIdx
② 塑性: 耐力点 index 〜 破断点 index
```

---

## 15. 解析パラメータ既定値

| パラメータ | 既定 | 用途 |
|---|---|---|
| `gaugeLength` | 50 mm | ひずみ算出 |
| `velocityThreshold` | 5.0 mm/min | 速度法耐力の初期閾値 |
| `linearStart_pct` | 5 % | 耐力探索の下限荷重（maxForce比） |
| `linearityLo` / `linearityHi` | 0.2 / 0.5 | 直線域フィットの応力バンド（耐力応力比） |
| `linearityR2Ok` / `linearityR2Warn` | 0.995 / 0.990 | 直線性の合格/要確認R² |
| `fracture_k1` | 5 | 式① 急落倍率 |
| `fracture_k2` | 0.02 | 式② 低荷重比 |
| `shimadzu pctOfMaxForce` | プリセット可変 | 島津法の1秒低下率% |
| `smoothingWindow` | 5 | 速度移動平均窓 |

- 全パラメータは設定UIで変更可能。変更時は全ファイル再解析。設定はJSONでImport/Export、localStorage保存に対応してよい（外部提出時はCDN同様、社内固有情報を含めない）。

---

## 16. 解析結果の出力

### 16.1 画面（1ファイル）

- 判定バナー（合格/要確認/不合格）。
- 主要値: 引張強さ Rm、耐力(速度法)、0.2%耐力、伸び、応力増加速度、（参考）ヤング率・ひずみ速度①②。
- 線図（SS曲線／時間-応力）とマーカー。

### 16.2 一覧表（複数ファイル集計）＋CSV

- 列: 試験日, ファイル名, 耐力(速度法)[N/mm²], 0.2%耐力[N/mm²], 引張強さ[N/mm²], 破断伸び[%], （島津伸び[%]）, ヤング率[N/mm²], 応力増加速度[MPa/s], ひずみ速度①[s⁻¹], ひずみ速度②[s⁻¹], 判定。
- 列ヘッダでソート、行クリックで単票表示。
- 「CSV出力」で UTF-8 BOM付きCSVをダウンロード（§6・§7 のDL要件に準拠。分割・Zip一括の両対応）。

---

## 17. 解析の受け入れ条件（§9 に追加）

- SS曲線・時間-応力線図が正しく描画され、耐力点▽・破断点×・0.2%耐力★が妥当な位置に表示される。
- サンプル（正常/セレーション/上降伏点）で Rm・耐力・伸び・応力増加速度が算出され、正常サンプルで耐力≒設定降伏応力、Rm≒設定UTS に整合する。
- ゲージ長・断面積・各パラメータ変更が即時に再解析へ反映される。
- 時刻列が無いデータでは、時間依存量（速度法耐力・応力増加速度・ひずみ速度）を「算出不可」と明示し、他の量（Rm・伸び）は算出を継続する。
- 応力の取得（§11.2）が入力経路で正しく切り替わること: CSV入力は行ごとの応力列を使用、DAT入力は force÷A で復元した全点SS応力が DAT内蔵のサマリー応力（最大点_応力等）と一致する。
